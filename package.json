{
  "name": "ory-kratos-ts-fetch-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "openapi-generator-cli generate -i openapi.json -g typescript-fetch -c ./typescript.yaml -o ./api"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.7.0"
  }
}
